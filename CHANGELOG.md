# 📝 DateCalculator (日期计算器) 版本演进与变更日志

本文档记录了 **DateCalculator (日期计算器)** 从 v1.0.0 初始版本发布至今的主要功能迭代、UI 设计演进以及缺陷修复历史。

---

## 🚀 v1.2.1 (2026-09-19)

### 🐛 缺陷修复 (Bug Fixes)
1. **DatePicker 文本输入光标截断修复**：
   - 彻底修复了在日历弹窗文本输入模式下（如 `2026/09/19`）修改年份时，光标无法定位在数字 `6` 后面的掩码截断 Bug（通过在 `rememberDatePickerState` 中显式指定 `1900..2100` 年份解析范围解决）。
2. **DatePicker 年份切换透明重叠修复**：
   - 彻底修复了切换至快速选年份网格视图时，年份数字与底层的月份日面发生透明重叠的显示 Bug（将容器背景切换为实体 `NeumorphicBg` 冰雪白）。
3. **消除下拉菜单残留的灰色直角/方角阴影**：
   - 彻底消除了下拉菜单弹出层底层残留的 Android 原生 Popup 灰色直角阴影（重构为纯物理 `NeumorphicCustomPopup`，内留 `10.dp` 柔影弥散边距）。
4. **消除底栏切页文字与 Icon 闪烁感**：
   - 彻底消除了底栏在手势滑动切页时文字/图标颜色的离散跳变与闪烁感（采用 `lerp` 实时线性颜色插值与恒定 `FontWeight.Bold`）。
5. **移除中国大陆地区说明中的年份字样**：
   - 移除地区选择说明中关于 `(2024-2026)` 的字样标注，保持界面长久简洁。

### ✨ 新增功能与优化 (Features & Enhancements)
1. **GPS 自动识别所在地 (开关化 + 运行时授权)**：
   - 重构为 3D 新拟物 Switch 开关（默认置为关闭状态），首次开启时主动弹窗询问定位运行时权限；授权后自动通过 GPS/网络定位匹配当前所在地节假日。
2. **全量支持 13 个国家与地区**：
   - 新增新加坡 🇸🇬、马来西亚 🇲🇾、越南 🇻🇳、澳大利亚 🇦🇺、新西兰 🇳🇿，并将台湾重命名为 `台湾（中国）` 🇹🇼；地区选择器重构为 `280.dp` 58dp 拟物下拉选择框。
3. **系统无障碍 (TalkBack 读屏) 深度适配**：
   - 全量补全语义标签 (`semantics`)、合并卡片朗读焦点（`mergeDescendants`），并将所有可点击按钮热区统一提升至 ≥48dp 规范。
4. **全量文案平民化与自然口语化**：
   - 全局遍历重构 13 处计算机硬核术语为直观口语（如 *“热门倒计时节日”*、*“+ 新增倒计时”*、*“选择起始日期”*、*“工作日与休假规则”*）。

---

## 🚀 v1.2.0 (2026-09-19)

### ✨ 新增功能 (New Features)
1. **自定义倒计时 (自选图标 + 周期推算)**：
   - 支持自选 12 种 Emoji 分类图标（`📌`, `🎂`, `💍`, `❤️`, `🚀`, `✈️`, `🎓`, `🏠`, `💰`, `🎁`, `⚽`, `🎮`）。
   - 支持周期性自动推算（`不重复`、`每周`、`每月`、`每年`），目标日期过期时自动按周期推算下一个目标日。
2. **添加系统提醒 (闹铃 / 日历 / App 通知)**：
   - 支持一键跳转系统闹铃设置界面（自动带入事件专属名称，不透传固定 9点，由用户自由选择时间）。
   - 支持跳转系统日历自动新建日程。
   - 支持 App 本地弹出通知提醒（配合 Android 13+ 运行时通知权限）。
3. **自动下滑与结果输出**：
   - 点击倒计时或快捷节日后，结果卡片展开并自动平滑下滑至视口区域。
4. **传统节日烟花动画**：
   - 农历转换选中中秋/春节等传统节日时，点击可触发 `FireworksAnimation` 动态烟花粒子散落视效。

### 🐛 缺陷修复 (Bug Fixes)
- 修复倒计时中秋节等目标节日工作日计算少算 1 天的区间算法漏洞。
- 修复首次切回农历转换页面时烟花重复播放的 Bug。
- 修复首次切回主页时 Hero Card 自动播放缩放 Bounce 动效的问题（改为仅点击快捷键时触发）。

---

## 🚀 v1.1.0 (2026-09-18)

### ✨ 新增功能 (New Features)
- **全量 3D 新拟物视觉系统 (Neumorphism Design System)**：
  - 重构全屏按键、输入框、分段选择器与卡片光影，采用 `neumorphicInset` 与 `neumorphicExtruded` 结合呈现视觉沉浸感。
- **多地区节假日数据库支持**：
  - 引入中国大陆、台湾、香港、澳门、日本、韩国、美国、泰国共 8 个地区法定节假日及调休补班数据。
- **计算历史记录管理**：
  - 新增计算日志查看、单条滑动删除与一键清空历史记录功能。

---

## 🚀 v1.0.0 (2026-09-17)

### 🎉 初始版本发布 (Initial Release)
- 完成工作日/自然日加减推算的底层核心算法。
- 完成 1900-2100 年农历万年历算法。
- 支持双休、大小周、单休、无休等基础工作日规则。
