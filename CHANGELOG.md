# 📝 DateCalculator (日期计算器) 版本演进与变更日志

本文档记录了 **DateCalculator (日期计算器)** 从 v1.0.0 初始版本发布至今的主要功能迭代、UI 设计演进以及缺陷修复历史。

---

## 🚀 v1.3.0 (2026-09-20) - 商业重磅升级

### ✨ 核心新功能 (New Key Features)
1. **支持全球 19 个主要国家/地区法定节假日及调休数据库**：
   - 在原有 13 个国家/地区基础上，新增**英国 🇬🇧、德国 🇩🇪、法国 🇫🇷、意大利 🇮🇹、印度 🇮🇳、印尼 🇮🇩**。
   - 彻底补全了所有地区全量法定节假日与国家重大节点（包含中国大陆的清明节、五一劳动节等）。
2. **📈 股市模式 (忽略调休补班)**：
   - 针对 A 股与金融机构交易场景，在“周末休假模式”的“双休”下方增加“📈 股市模式”附加开关（默认关闭）。
   - 开启后，遭遇国务院调休补班日（如 9月20日周日）强制按正常双休/休市算，不强行作为工作日，完美契合金融与股市排期需求。
3. **设置项持久化存储记忆**：
   - 引入 `PreferenceUtils` 本地 SharedPreferences 存储，用户选择的地区、周末模式、大小周配置、GPS 开关及股市模式在退出并重启应用后 **100% 自动保持恢复记忆**。
4. **常用倒计时节日配置**：
   - 设置中增加“常用倒计时节日配置”板块，用户可自由开启或隐藏任意预设节日按键；且两页节日列表 100% 同源对齐。

### 🎨 界面与交互细节精修 (UI & UX Refinements)
1. **日期倒计时页面结构重构**：
   - 将 **`目标日期`** 功能区调至上方，**`常用目标日期`** 调至下方，提升逻辑顺畅度。
   - 常用目标日期功能区的 **`+ 自定义`** 按键移至所有节日 Chip 列表的最后一个位置。
   - 精简重命名：`常用节日倒计时` ➔ **`常用目标日期`**；`+ 新增倒计时` ➔ **`+ 自定义`**；`目标特定日期` ➔ **`目标日期`**；`起始基准日` ➔ **`起始日期`**。
2. **一屏完整展现计算结果**：
   - 优化日期计算页面规整间距，按等于号或切换快捷键后，页面自动平滑下滑至结果卡片完全展现位置，无需用户手动下滑。
3. **结构化历史记录卡片重构**：
   - 全面适配 `18.dp` 柔和圆角（`RoundedCornerShape(18.dp)`），彻底消除方角边缘。
   - 顶部增加分类彩色胶囊 Pill（`日期计算` 蓝 / `日期倒计时` 紫 / `农历公历` 绿）。
   - 核心结果大字加粗高亮展示，计算过程结构化拆解呈现，并附带地区与时间页脚。
4. **小标题规格全局统一**：
   - “计算历史”与“工作日与休假规则”小标题行统一为 `36.dp` 恒定高度与 `20.dp` 蓝标 Icon，实现两标签页切页时标题位置绝对静止对齐。
5. **股市模式开关 UI 精细对齐**：
   - 消除多行重叠说明字样，Switch 开关按 `0.75x` 比例精细缩小，与左侧 `13sp` 标题字体完美等高对齐。

---

## 🚀 v1.2.1 (2026-09-19)

### ✨ 新增功能与优化 (Features & Enhancements)
1. **GPS 自动识别所在地 (开关化 + 运行时授权)**：
   - 重构为 3D 新拟物 Switch 开关（默认开启），首次开启时主动弹窗询问定位运行时权限。
2. **全量支持 13 个国家与地区**：
   - 地区选择器重构为 `280.dp` 58dp 拟物下拉选择框（解决卡片过窄问题）。
3. **系统无障碍 (TalkBack 读屏) 深度适配**：
   - 全量补全语义标签 (`semantics`)、合并卡片朗读焦点（`mergeDescendants`），并将所有可点击按钮热区统一提升至 ≥48dp 规范。

---

## 🚀 v1.2.0 (2026-09-19)

### ✨ 新增功能 (New Features)
1. **自定义倒计时 (自选图标 + 周期推算)**：
   - 支持自选 12 种 Emoji 分类图标（`📌`, `🎂`, `💍`, `❤️`, `🚀`, `✈️`, `🎓`...）。
   - 支持周期性自动推算（`不重复`、`每周`、`每月`、`每年`）。
2. **添加系统提醒 (闹铃 / 日历 / App 通知)**：
   - 支持跳转系统闹钟、系统日历及 App 本地弹出通知提醒。
3. **传统节日烟花动画**：
   - 农历转换选中中秋/春节等传统节日时，点击可触发 `FireworksAnimation` 动态烟花粒子散落视效。

---

## 🚀 v1.1.0 (2026-09-18)

### ✨ 新增功能 (New Features)
- 全量 3D 新拟物视觉系统 (Neumorphism Design System)。
- 引入中国大陆、台湾、香港、澳门、日本、韩国、美国、泰国共 8 个地区法定节假日及调休补班数据。

---

## 🚀 v1.0.0 (2026-09-17)

### 🎉 初始版本发布 (Initial Release)
- 完成工作日/自然日加减推算的底层核心算法。
- 完成 1900-2100 年农历万年历算法。
