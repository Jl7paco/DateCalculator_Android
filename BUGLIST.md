# 🛠️ 日期计算器 (DateCalculator) 全量缺陷修复与问题记录清单 (v1.0.0 ➔ v2.1.0)

本文档整理记录了 **日期计算器 (DateCalculator)** 项目自 v1.0.0 初始版本发布至今，在各个版本迭代中遇到并解决的已知缺陷与修改记录。

---

## 📋 按版本演进分类缺陷清单

### 五、 v2.1.0 体验优化、老黄历宜忌与界面精修版本

#### Bug #29: 老黄历宜忌项目随日期切换时结果重复未变化
- **涉及文件**：`LunarCalendarUtils.kt`
- **现象描述**：切换公历日期时，老黄历中的“宜”和“忌”选项内容没有随之发生改变。
- **根因分析**：早期的哈希计算公式取余时在同月份内产生了相同的余数。
- **解决方案**：引入基于 `toEpochDay` 的同余杂凑算法，确保相邻日期及跨月日期的宜忌推荐实时动态更新。

#### Bug #30: “宜”与“忌”视觉配色对比不清晰
- **涉及文件**：`LunarConverterScreen.kt`
- **现象描述**：“宜”与“忌”的色彩搭配与新拟物底色融合度不高，且两者间对比度偏低。
- **根因分析**：使用了过于鲜艳的红色与绿色硬编码色值。
- **解决方案**：“宜”采用积极鲜艳的翡翠绿（`#10B981`），“忌”采用低调收敛的沉稳冷灰（`#64748B`），形成直观对比。

#### Bug #31: 多段模式卡片展开/收起时出现掉帧
- **涉及文件**：`DateCalculationScreen.kt`
- **现象描述**：展开或收起多段模式卡片时，画面有轻微掉帧和卡顿感。
- **根因分析**：`expandVertically` 动画过程中，内部多个子组件在每一帧都被重新测量布局。
- **解决方案**：引入 GPU 硬件离屏层缓存（`CompositingStrategy.Offscreen`），避免动画过程中的 CPU 逐帧重测，保证 60/120 FPS 流畅度。

#### Bug #32: 多段模式外层卡片阴影在部分边缘呈方形
- **涉及文件**：`DateCalculationScreen.kt` 与 `TimelineDiagram.kt`
- **现象描述**：多段模式卡片右上角和边缘阴影呈现直角方形。
- **根因分析**：在 `neumorphicExtruded` 之前错误地使用了 `.clipToBounds()`，导致外扩的阴影被直角裁剪。
- **解决方案**：移除了阴影绘制层的 `.clipToBounds()` 限制，并使用 `Surface` 预留外扩边距，恢复平滑圆角阴影。

#### Bug #33: 历史记录详情弹窗内容挤压且存在重复按钮
- **涉及文件**：`HistoryScreen.kt` 与 `TimelineDiagram.kt`
- **现象描述**：点击多段历史记录查看详情时，弹窗偏窄，且弹窗内部和标题栏各有一个“导出 CSV”按钮。
- **根因分析**：弹窗使用了系统默认宽度，且图表组件内部自带了导出按钮。
- **解决方案**：将详情弹窗调宽至 92% 屏宽，只保留标题栏唯一的“导出 CSV”按键，并移除了第二层嵌套卡片边框。

#### Bug #34: 节气表达前缀冗余与 Icon 图标静态未跟随季节切换
- **涉及文件**：`LunarCalendarUtils.kt` 与 `LunarConverterScreen.kt`
- **现象描述**：二十四节气提示词带有冗余的前缀 `"节气："`，且全部使用了单一静态 Icon `🌿`。
- **根因分析**：未对 24 个节气的四季属性进行图标映射匹配。
- **解决方案**：移除了前缀 `"节气："`，并根据 24 节气名称动态匹配四季专属 Emoji（如 `🌸 春分` / `🏖️ 夏至` / `🌾 秋分` / `🥟 冬至`）。

#### Bug #35: 主视图滑页偶尔出现停顿与黏连划不动
- **涉及文件**：`MainScreen.kt`
- **现象描述**：在主视图左右划动切页时，有时会像被东西黏住一样速度降为 0 划不动。
- **根因分析**：底栏手势监听函数中调用了 `change.consume()`，强行将拖拽事件标记为已消费，斩断并取消了 Pager 的惯性滑动动画。
- **解决方案**：移除了底栏拖拽手势中的 `change.consume()` 抢占限制，使触摸事件正常透传，彻底解决了切页黏连卡死问题。

---

### 一、 v1.0.0 / v1.1.0 基础框架与历法核心算法版本

#### Bug #01: 拟物卡片边缘阴影在屏边缘被直接裁剪
- **涉及文件**：`DateCalculationScreen.kt`
- **现象描述**：3D 新拟物卡片的软阴影在屏幕左右边缘被断开。
- **解决方案**：在布局边距中增加 4.dp 弥散缓冲区。

#### Bug #02: 农历正月初一前的日期干支与生肖属算错
- **涉及文件**：`LunarCalendarUtils.kt`
- **现象描述**：农历正月初一之前的公历日期，干支和生肖提前跳转。
- **解决方案**：严格以农历正月初一作为生肖与干支纪年的切换节点。

#### Bug #03: 国务院调休补班与周末撞期导致工作日重复计算
- **涉及文件**：`DateCalculatorUtils.kt`
- **现象描述**：调休补班日为周末时，既扣除了周末又加回了补班。
- **解决方案**：建立明确的优先级判定链：调休补班 > 法定节假日 > 周末规则。

---

### 二、 v1.2.0 / v1.2.1 自定义倒计时与系统提醒版本

#### Bug #06: 自定义重复倒计时推算过期事件失效
- **涉及文件**：`DateCalculatorViewModel.kt`
- **现象描述**：按年重复的节日过期后，倒计时呈现负数。
- **解决方案**：增加目标日期自动推进算法，自动推进至下一个周期。

#### Bug #07: 运行时定位权限被拒绝后引发应用崩溃
- **涉及文件**：`SettingsScreen.kt`
- **现象描述**：拒绝定位权限时，应用报错退出。
- **解决方案**：捕获拒绝状态，提示用户并降级为手动选择模式。

---

### 三、 v1.3.0 股市模式与多地区假期版本

#### Bug #11: 股市模式下遇调休补班日误强加工作日
- **涉及文件**：`Models.kt` 与 `DateCalculatorUtils.kt`
- **现象描述**：金融与 A 股交易场景下，调休补班日误算为工作日。
- **解决方案**：增加“股市模式”开关，开启后忽略调休补班，按正常周末休市算。

---

### 四、 v2.0.0 科学计算器模式与区间拆算版本

#### Bug #16: 多段模式下删空默认 15 后输入 10 变成 100
- **涉及文件**：`Models.kt` (`CalculationStage`)
- **现象描述**：删除默认的 15 后输入 10 变成 100。
- **解决方案**：允许输入框呈现为空串 `""`，仅在计算时对空串进行兜底解析。

#### Bug #17: 多段模式输入框焦点高亮问题
- **涉及文件**：`DateCalculationScreen.kt` (`InsertDaysTextField`)
- **现象描述**：未点击输入框时带有蓝色高光全选条。
- **解决方案**：未聚焦时不显示高光选中条，点击聚焦时高亮全选。
