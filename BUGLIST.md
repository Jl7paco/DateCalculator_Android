# 🛠️ 日期计算器 (DateCalculator) 全量缺陷修复与问题记录清单 (v1.0.0 ➔ v2.0.0)

本文档详尽记录了 **日期计算器 (DateCalculator)** 项目自 v1.0.0 初始版本发布至今，在各个版本迭代中遇到并解决的全部已知缺陷与细节优化。

---

## 📋 按版本演进分类缺陷清单

### 一、 v1.0.0 / v1.1.0 基础框架、拟物 UI 与历法核心算法版本

#### Bug #01: 拟物卡片边缘阴影在屏边缘被直接裁剪
- **涉及文件**：`DateCalculationScreen.kt`
- **现象描述**：3D 新拟物卡片的软阴影在屏幕左右边缘被断开，缺乏自然扩散过渡。
- **根因分析**：外层容器没有预留足够的阴影弥散 Padding 空间。
- **解决方案**：在 `LazyColumn` / `Column` 布局边距中增加 4.dp 弥散缓冲区。

#### Bug #02: 农历正月初一前的日期干支与生肖属算错
- **涉及文件**：`LunarCalendarUtils.kt`
- **现象描述**：农历正月初一之前的公历日期，显示的干支和生肖提前跳转到了下一年。
- **根因分析**：公历年份与农历干支纪年的节点未对齐，误按公历 1 月 1 日换算干支。
- **解决方案**：重构农历历法基准，严格以农历正月初一作为生肖与干支纪年的切换节点。

#### Bug #03: 国务院调休补班与周末撞期导致工作日重复计算
- **涉及文件**：`DateCalculatorUtils.kt`
- **现象描述**：遇到如五一/国庆调休补班日为周六或周日时，系统既扣除了周末又加回了补班，造成计算逻辑混淆。
- **根因分析**：工作日判定缺乏明确的优先级层级。
- **解决方案**：建立明确的优先级判定链：调休补班 > 法定节假日 > 周末规则。

#### Bug #04: 跨年自然日与工作日区间天数计算包含首尾边界逻辑偏差
- **涉及文件**：`DateCalculatorUtils.kt`
- **现象描述**：在进行跨年或跨月日期推算时，计算结果相比实际天数多出或少出 1 天。
- **根因分析**：边界日（起始日）是否包含在计数循环内的逻辑在不同计算模式下未完全对齐。
- **解决方案**：统一全应用边界处理原则：起始当日作为基准起点，计算从基准日次日开始累加，确保自然日与工作日推算逻辑完全一致。

#### Bug #05: 按钮点击快速连续触发导致重复算天与状态抖动
- **涉及文件**：`DateCalculatorViewModel.kt`
- **现象描述**：快速连续点击“等于”或快捷选天胶囊按钮时，界面结果卡片出现频繁跳动与重复历史记录写入。
- **根因分析**：UI 触发层缺少防抖动与计算状态收敛处理。
- **解决方案**：在 ViewModel 中对计算事件进行防抖控制，并在状态更新前先收敛历史记录的非重复判定。

---

### 二、 v1.2.0 / v1.2.1 自定义倒计时、系统提醒与 GPS 定位拓展版本

#### Bug #06: 自定义重复倒计时（每周/每月/每年）推算过期事件失效
- **涉及文件**：`DateCalculatorViewModel.kt` (`CustomEventItem`)
- **现象描述**：当设置了按年重复的节日（如生日 2月14日），如果选中的起始日期已超过当年 2月14日，倒计时呈现负数相差天数。
- **根因分析**：目标日期未随时间流逝自动推算至下一个周期。
- **解决方案**：增加目标日期自动推进算法，自动将过去的日期推进至未来下一个周期的对应日期。

#### Bug #07: 运行时定位权限被拒绝后引发应用崩溃
- **涉及文件**：`SettingsScreen.kt`
- **现象描述**：首次开启“GPS 自动识别所在地”时，如果用户拒绝定位权限，应用报错退出。
- **根因分析**：缺乏对权限拒绝状态的捕获处理。
- **解决方案**：使用 Compose 动态权限 Launcher 捕获拒绝状态，提示用户并优雅降级为手动选择模式。

#### Bug #08: 地区选择下拉菜单宽度过窄导致文本换行挤压
- **涉及文件**：`NeumorphicComponents.kt`
- **现象描述**：设置页面的地区选择下拉菜单宽度受限，国旗和地区名称挤在一起发生换行。
- **根因分析**：系统默认下拉菜单受父容器宽度限制。
- **解决方案**：自定义悬浮组件，固定设置为 280dp 宽度与 320dp 滚动列表。

#### Bug #09: 添加系统日历日程与闹钟提醒时事件名称丢失字符
- **涉及文件**：`NotificationUtils.kt`
- **现象描述**：将倒计时事件一键同步至系统日历或闹钟时，带有 Emoji 前缀的名称出现截断或乱码。
- **根因分析**：Intent 传递 Extra 参数时未对前缀 Emoji 进行 UTF-8 字符边界对齐。
- **解决方案**：优化 Intent 数据提取逻辑，对 Emoji 与字符串拼接进行安全隔离，确保系统日历与闹钟接收到完整标语。

#### Bug #10: 农历传统佳节烟花粒子效果在部分小屏设备上遮挡底栏按键
- **涉及文件**：`FireworksAnimation.kt`
- **现象描述**：触发中秋/春节烟花动画时，粒子全屏散落导致底部导航栏短时间内无法点击。
- **根因分析**：烟花 Canvas 层的触摸事件拦截（`pointerInput`）透传设置不当。
- **解决方案**：将烟花 Canvas 的触摸事件设置为非阻塞（`pointerInput` 设为透明穿透），保证烟花绽放的同时用户依然可以流畅切页。

---

### 三、 v1.3.0 股市模式、多地区假期与多段连算初版

#### Bug #11: 股市模式下遇调休补班日误强加工作日
- **涉及文件**：`Models.kt` 与 `DateCalculatorUtils.kt`
- **现象描述**：金融与 A 股交易场景下，遭遇国务院调休补班日（如周日）时，系统错误地将其当作工作日。
- **根因分析**：未区分普通行政排期与股市休市规则。
- **解决方案**：增加“股市模式”全局开关，开启后强制忽略调休补班，按正常周末休市算。

#### Bug #12: “计算加减天数”小标题双层重复渲染
- **涉及文件**：`NumericCalculatorInput.kt`
- **现象描述**：主页面出现了上下两层重叠的“计算加减天数”标题。
- **根因分析**：外部页面容器与组件内部都写了一次标题。
- **解决方案**：删除了输入组件内部的重复标题代码，保留外层统一布局。

#### Bug #13: 远期（2027 年及以后）节假日与调休补班缺失
- **涉及文件**：`RegionalHolidays.kt`
- **现象描述**：当推算跨度超过 150 天至 2027 年及以后时，时间轴无法显示远期的节假日与调休补班。
- **根因分析**：官方放假通知通常仅发布至当年/次年，缺乏远期数据。
- **解决方案**：建立公历/农历节假日预测算法模型，预测远期节日及调休，并在图表区附带预测说明横幅。

#### Bug #14: 多地区法定节假日数据库缺失部分国家重大事件与补假
- **涉及文件**：`RegionalHolidays.kt`
- **现象描述**：切换至英国、德国、法国、台湾地区时，部分年份的替补休假（Bank Holiday / 补假）未被精准扣除。
- **根因分析**：早期假库仅录入了主要正节，未覆盖替代公休假（Substitute Holiday）。
- **解决方案**：全量补充 19 个国家和地区的替代公休假与补假日期字典，确保全球各地区工作日扣除 100% 精确。

#### Bug #15: 设置项（地区/大小周/股市模式）退出应用后无法保存记忆
- **涉及文件**：`PreferenceUtils.kt`
- **现象描述**：用户在设置中选好了大小周或开启了股市模式，重启应用后配置恢复默认。
- **根因分析**：状态仅保存在内存 ViewModel 中，未持久化写入磁盘。
- **解决方案**：引入 `PreferenceUtils` 工具类，将地区、周末模式、大小周与股市模式实时写入 `SharedPreferences`，启动时自动读取恢复。

---

### 四、 v2.0.0 科学计算器模式、区间拆算与交互精修版本

#### Bug #16: 多段模式下删空默认 15 后输入 10 变成 100
- **涉及文件**：`Models.kt` (`CalculationStage`)
- **现象描述**：删除默认的 15 后打字输入 10，结果框变成 100；输入 2 变成 20。
- **根因分析**：输入框清空时，内部数值被默认解析设为了数字 0，导致打字时拼接在 0 后面（例如 `"0" + "10"` 变成 `"100"`）。
- **解决方案**：天数内部存为字符串，允许输入框呈现为空串 `""`，只有在计算时才对空串进行兜底解析。

#### Bug #17: 多段模式输入框焦点高亮与覆盖替代 (Insert Mode)
- **涉及文件**：`DateCalculationScreen.kt` (`InsertDaysTextField`)
- **现象描述**：未点击输入框时带有蓝色高光全选条，且打字时无法直接替代默认数字。
- **根因分析**：`BasicTextField` 初始选中范围未与输入框的点击/聚焦状态动态联动。
- **解决方案**：未聚焦时不显示高光选中条；点击聚焦时全选高亮默认数值；开始打新数字时自动替换高亮并恢复正常光标。

#### Bug #18: 黑暗模式下默认数字 15 对比度不足
- **涉及文件**：`DateCalculationScreen.kt`
- **现象描述**：在系统夜间模式下，默认的 15 灰色占位字透明度较低，看不清楚。
- **根因分析**：使用了固定透明度的灰色，未适应深色背景。
- **解决方案**：根据日间与夜间模式动态调整颜色（日间采用 `#475569`，夜间采用 `#94A3B8`），确保两种模式下都能清晰识别。

#### Bug #19: 启动 App 时重复触发数据同步并弹出 Toast
- **涉及文件**：`DateCalculatorViewModel.kt`
- **现象描述**：打开应用时，屏幕会连续弹两次“已自动同步最新节假日数据”的提示。
- **根因分析**：配置初始化与 GPS 自动定位链路重复调用了同步函数。
- **解决方案**：增加静默同步标记，应用启动时仅在后台静默同步数据，不弹 Toast 提示。

#### Bug #20: 区间拆算结果卡片底部复制按钮与文本排版重叠
- **涉及文件**：`RangeBreakdownCard.kt`
- **现象描述**：拆算结果卡片底部，“复制结果”文字按键与左侧地区文本挤在一起。
- **根因分析**：使用了固定宽度的文字按键，挤压了空间。
- **解决方案**：将“复制结果”改为 32dp 的图标按键，左侧文字设置弹性权重，解决文本重叠。

#### Bug #21: 区间拆算模式下“多段模式”按键状态混淆
- **涉及文件**：`DateCalculationScreen.kt`
- **现象描述**：处于“区间拆算”模式时，“多段模式”按键仍能点击，导致逻辑冲突。
- **根因分析**：区间拆算与多段模式属于不同的计算逻辑，不应同时开启。
- **解决方案**：在区间拆算模式下，将“多段模式”胶囊按键直接置灰且不可点击，同时收起展开的多段卡片。

#### Bug #22: 多段减法推算时底部节点文字误显示“最终完成日期”
- **涉及文件**：`TimelineDiagram.kt`
- **现象描述**：多段减法模式向过去倒推时，最下方的最终节点仍显示“最终完成日期”。
- **根因分析**：未根据减法模式调整结尾节点的语义。
- **解决方案**：多段减法模式下，结尾节点自动更名为“最初开始日期”，图标切换为沙漏 `⏳`。

#### Bug #23: 多段模式卡片展开与折叠时边框闪烁与跳动
- **涉及文件**：`DateCalculationScreen.kt`
- **现象描述**：展开或收起多段卡片时，内容出现闪烁与生硬的跳动。
- **根因分析**：使用了较快的线性动画，未对边界进行防溢出裁剪。
- **解决方案**：改用低阻尼弹簧物理动效，并加上边界防溢出裁剪，使展开和收起动作平滑自然。

#### Bug #24: 主界面加减状态与多段模式内部加减不一致
- **涉及文件**：`DateCalculatorViewModel.kt`
- **现象描述**：主界面选择了“-”减法，展开多段模式后第一段时间默认仍是“+”加法。
- **根因分析**：多段模式展开时未获取主界面当前的运算符状态。
- **解决方案**：展开多段模式时，自动继承主界面当前选中的运算符，且多段模式内部保持统一的加或减逻辑。

#### Bug #25: 清空历史记录二次确认弹窗样式不统一
- **涉及文件**：`HistoryScreen.kt`
- **现象描述**：清空历史记录时弹出的对话框使用了系统默认白色扁平样式。
- **根因分析**：直接使用了未经样式自定义的对话框组件。
- **解决方案**：重构二次确认弹窗，使用新拟物背景、圆角容器与新拟物按键。

#### Bug #26: 历史与设置页面字号相比其他页面偏大
- **涉及文件**：`SettingsScreen.kt`
- **现象描述**：第四页“历史与设置”的小标题和卡片字号相比前三个页面偏大。
- **根因分析**：部分标题使用了不一致的 `sp` 字号。
- **解决方案**：全应用统一字体规范：15sp 页面标题、14sp 卡片标题、13sp 选项文本、11sp 说明文字。

#### Bug #27: 切换底部导航标签页时图标颜色突变与切页卡顿
- **涉及文件**：`MainActivity.kt`
- **现象描述**：在底部 4 个标签页之间点击切换时，图标高亮过渡不平滑。
- **根因分析**：状态切换时图标颜色直接跳变。
- **解决方案**：在导航栏动画层引入动态颜色插值，实现平滑颜色渐变。

#### Bug #28: 多段模式下按键“添加下一段时间”附带多余 `+` 图标
- **涉及文件**：`DateCalculationScreen.kt`
- **现象描述**：“添加下一段时间”按键文本前面带有重复多余的 `+` 符号与图标。
- **根因分析**：布局中同时声明了 `Icon(Icons.Default.Add)` 与字符串前缀 `"+ "`。
- **解决方案**：移除多余图标与前缀，文本保持简洁统一的 `"添加下一段时间"`。
