# 🛠️ 日期计算器 (DateCalculator) 全量缺陷修复与问题清单 (v1.0.0 ➔ v2.0.0 Bug List)

本文档汇总整理了 **日期计算器 (DateCalculator)** 项目自 v1.0.0 初始版本发布至今，在 v1.1.0、v1.2.0、v1.3.0 及 v2.0.0 各个大版本迭代中遇到的全量缺陷报告、根因分析以及技术修复方案。

---

## 📋 按版本演进分类缺陷清单

### 一、 v1.0.0 / v1.1.0 基础与历法算法缺陷 (Early Foundation Fixes)

#### Bug #01: 拟物卡片底部阴影与 Edge 视口边缘裁剪变形
- **现象描述**：3D 新拟物卡片的阴影在屏边缘被直接切断，缺乏软扩散效果。
- **根因分析**：容器外层没有预留足够的弥散 Padding 空间。
- **解决方案**：在 `LazyColumn` / `Column` 布局边距中增加 `padding(horizontal = 4.dp)` 弥散缓冲区。

#### Bug #02: 农历闰月与公历年份干支跨年计算偏差
- **现象描述**：农历正月初一之前的公历日期干支属相提前跳转至下一年。
- **根因分析**：公历年份与农历干支纪年的节点未严格对齐。
- **解决方案**：重构 `LunarCalendarUtils` 农历历法基准，严格以农历正月初一作为生肖与干支纪年的切换节点。

#### Bug #03: 国务院调休补班与传统周末撞期导致工作日双重扣除
- **现象描述**：遇到如五一/国庆调休补班日为周六或周日时，系统既扣除了周末又加回了补班，造成计算逻辑混淆。
- **根因分析**：工作日判定优先级缺乏明确层级。
- **解决方案**：在 `DateCalculatorUtils.isWorkday` 逻辑中建立优先判定链：调休补班 `isShiftWorkday` > 法定节假日 `isStatutoryHoliday` > 周末规则 `isWeekend`。

---

### 二、 v1.2.0 / v1.2.1 倒计时与定位扩展缺陷 (Countdown & GPS Fixes)

#### Bug #04: 自定义倒计时重复周期（每周/每月/每年）推算过期事件失效
- **现象描述**：当设置了按年重复的节日（如生日 2月14日），如果选中的起始日期已超过当年 2月14日，倒计时呈现负数相差天数。
- **根因分析**：目标日期未随时间流逝自动滚动递进至下一周期。
- **解决方案**：在 `CustomEventItem` 中引入 `getNextUpcomingDate()` 滚动算法，自动将过去的目标日期循环推进至未来下一个对应周期。

#### Bug #05: 运行时定位权限拒接后 GPS 开关崩溃或卡死
- **现象描述**：首次开启 GPS 自动识别时，若用户拒绝定位权限，应用抛出权限异常。
- **根因分析**：缺乏运行时动态权限捕获与优雅降级。
- **解决方案**：引入 Compose `rememberLauncherForActivityResult` 运行时动态权限申请，在权限被拒绝时优雅降级并弹窗引导，不崩溃卡死。

#### Bug #06: 地区下拉菜单被卡片宽度限制导致选项挤压
- **现象描述**：设置页面的地区选择下拉菜单宽度过于狭窄，导致旗帜与地区中文名称被换行挤压。
- **根因分析**：内置的 `DropdownMenu` 默认受到父卡片宽度的绝对限制。
- **解决方案**：自定义 `NeumorphicCustomPopup` 悬浮弹出层，固定设置 `280.dp` 宽度与 `320.dp` 滚动高度。

---

### 三、 v1.3.0 / v2.0.0 多段排期与交互精修缺陷 (Multi-Stage & UI Fixes)

#### Bug #07: 多段模式下删空天数再打字数值异常 (10 变 100 / 2 变 20)
- **现象描述**：在多段模式输入框中，删除默认数字 `15` 后直接输入 `10` 会变成 `100`，输入 `2` 会变成 `20`。
- **根因分析**：输入框空串 `""` 被强制解析并归零 `0L`，导致新输入的数字拼接在默认 `0` 后面（如 `"0" + "10"` 变 `"100"`）。
- **解决方案**：重构 `CalculationStage` 数据模型，天数存储为 `daysInput: String` 字符串并允许空串，计算时通过 `daysInput.toLongOrNull() ?: 15L` 智能兜底。

#### Bug #08: 多段模式输入框焦点高亮与覆盖模式 (Insert Mode) 视效问题
- **现象描述**：未点击输入框时出现不必要的高光蓝色条带，且打字时无法直接覆盖替代默认的 `15`。
- **根因分析**：`BasicTextField` 初始 `selection` 范围未跟随 `onFocusChanged` 动态切换。
- **解决方案**：自定义 `InsertDaysTextField` 组件，结合 `onFocusChanged` 实现：未聚焦时淡灰色显示无高光；点击聚焦时全选高亮整个 `15`；打字输入后高光自动消失并转换为正常深色文本。

#### Bug #09: 黑暗模式下默认 15 灰色占位文字对比度过低不清晰
- **现象描述**：在系统 Dark Mode 下，默认的 `15` 占位符颜色发暗，难以看清。
- **根因分析**：静态 `alpha` 颜色的透明度在深色 3D 沉降背景上对比度不足。
- **解决方案**：引入 `isSystemInDarkTheme()` 动态色彩，日间模式采用 `#475569`，夜间模式采用 `#94A3B8`，达到 WCAG AAA 级别的高对比度。

#### Bug #10: 启动 App 时重复触发放假安排同步并弹出 Toast
- **现象描述**：打开 App 时跳出两次同步 Toast 弹窗打扰用户。
- **根因分析**：`initPreferences` 与 `isGpsAutoDetectEnabled` 的初始化链重复调用了 `autoSyncHolidayData`。
- **解决方案**：为 `autoSyncHolidayData` 增加 `showToast: Boolean` 标记，App 启动时执行静默后台同步（不弹 Toast），仅在用户手动切换地区时才弹出提示。

#### Bug #11: 拆算结果卡片复制按钮与“节假日标准”文本重叠
- **现象描述**：在“区间拆算”结果卡片底部，复制按钮文本过长，与左侧“节假日标准: 🇨🇳 中国大陆”排版重叠。
- **根因分析**：底栏使用了固定宽度的文字按钮，缺乏弹性的布局权重与字号限制。
- **解决方案**：将“复制结果”重构为 32.dp 独立 3D 拟物图标按钮（`Icons.Default.ContentCopy`），左侧文本添加 `weight(1f)`，彻底消除排版重叠。

#### Bug #12: 区间拆算模式下多段模式按钮状态混淆
- **现象描述**：在“区间拆算”反向推算模式下，多段模式按钮仍可点击或隐去，导致模式冲突。
- **根因分析**：区间拆算为固定双日期区间算法，与多段连续链式算法属于不同维度。
- **解决方案**：当处于“区间拆算”模式时，右侧“多段模式”胶囊按钮保持可见但自动**置灰 (greyed out)** 且设置为不可点击，并自动收起展开的多段卡片。

#### Bug #13: “计算加减天数”小标题双层重复渲染
- **现象描述**：主页面出现了上下两层重叠的“计算加减天数”标题。
- **根因分析**：外部布局容器与 `NumericCalculatorInput` 内部均包含了 `Text("计算加减天数")`。
- **解决方案**：移除 `NumericCalculatorInput` 内部的重复标题，实现单层统一渲染。

#### Bug #14: 清空历史记录二次确认弹窗风格不统一
- **现象描述**：清空历史记录弹窗使用了系统默认的 Material 3 白色扁平风格，与全应用 3D 新拟物美学不一致。
- **根因分析**：直接使用了未经スタイリング自定义的 `AlertDialog` 容器。
- **解决方案**：重构 `HistoryScreen` 的 `AlertDialog`，应用 `NeumorphicBg` 容器、`RoundedCornerShape(24.dp)` 圆角、红/灰色 3D 凸起胶囊按钮与高对比度文本。

#### Bug #15: 远期（2027年及以后）节假日缺失与调休断层
- **现象描述**：推算跨度超过 150 天至 2027 年及以后时，时间轴上无法显示远期节假日与调休补班。
- **根因分析**：国务院官方放假通知通常仅发布至当年/次年，缺乏远期数据。
- **解决方案**：构建传统公历/农历智能节假日预测算法模型，预测远期元旦、春节、清明、五一、端午、中秋、国庆及调休周末，并附带 `⚠️ 远期节假日安排包含智能算法预测` 提示横幅。

#### Bug #16: 历史与设置页面字号规格不统一
- **现象描述**：第四页“历史与设置”小标题与卡片字号相比前三个页面偏大。
- **根因分析**：部分组件使用了 `titleMedium` 或不一致的 `sp` 字号。
- **解决方案**：全应用统一字体规范：15sp 页面小标题、14sp 卡片标题、13sp 选项文本、11sp 辅助说明。

#### Bug #17: 股市模式下遇调休补班日错误加算工作日
- **现象描述**：金融与 A 股交易场景下，遭遇国务院调休补班日（如 9月20日周日）时系统强行将其计入工作日。
- **根因分析**：未区分普通行政办公排期与金融股市休市规理。
- **解决方案**：增加 `disableChinaShiftWorkdays: Boolean` 全局模式开关，开启后强制忽略调休补班，按正常休市算。

#### Bug #18: 切换导航标签页时底部 NavigationBar 渐变闪烁
- **现象描述**：在底栏 4 个标签页（日期计算/倒计时/农历/设置）切换时，高亮图标卡顿闪烁。
- **根因分析**：状态重构导致图标颜色突变。
- **解决方案**：在 Compose 动画层采用 `lerp` 动态颜色插值器，实现 100% 连续丝滑渐变。
